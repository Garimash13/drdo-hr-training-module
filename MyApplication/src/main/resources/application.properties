# Application Name
spring.application.name=DRDO-HR-Portal

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/drdo_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Sonudeepa81#

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.web.resources.static-locations=file:uploads/


# Server Configuration
server.port=8080

# CORS Configuration (Optional if using @CrossOrigin in controller)
# Add this only if you want global config, not per-controller
# See WebConfig.java if needed

# Logging (Optional: helps with debugging)
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
